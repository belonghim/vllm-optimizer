apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vllm-optimizer-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm-optimizer-backend
  labels:
    app: vllm-optimizer-backend
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: vllm-optimizer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vllm-optimizer-backend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: vllm-optimizer-backend
        app.kubernetes.io/part-of: vllm-optimizer
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/api/metrics"
    spec:
      serviceAccountName: vllm-optimizer-backend
      imagePullSecrets:
        - name: quay-pull-secret
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: backend
          image: quay.io/joopark/vllm-optimizer-backend:dev
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop: ["ALL"]
            seccompProfile:
              type: RuntimeDefault
          ports:
            - name: http
              containerPort: 8000
          envFrom:
            - configMapRef:
                name: vllm-optimizer-config
            - secretRef:
                name: vllm-optimizer-secret
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: CA_BUNDLE
              valueFrom:
                configMapKeyRef:
                  name: vllm-optimizer-config
                  key: CA_BUNDLE
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 12
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          volumeMounts:
            - name: tmp-dir
              mountPath: /tmp
            - name: vllm-optimizer-data
              mountPath: /data

      dnsConfig:
        searches:
          - vllm-optimizer-dev.svc.cluster.local
          - svc.cluster.local
          - cluster.local
        options:
          - name: ndots
            value: "5"
      dnsPolicy: ClusterFirst

      volumes:
        - name: tmp-dir
          emptyDir:
            sizeLimit: 100Mi
        - name: vllm-optimizer-data
          persistentVolumeClaim:
            claimName: vllm-optimizer-data
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: vllm-optimizer-backend
                topologyKey: kubernetes.io/hostname
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: vllm-optimizer-backend
  labels:
    app: vllm-optimizer-backend
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: backend-tls-cert
spec:
  selector:
    app: vllm-optimizer-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: vllm-optimizer-backend-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: vllm-optimizer-backend
  minReplicas: 1
  maxReplicas: 1  # RWO PVC requires single replica
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
      - type: Pods
        value: 2
        periodSeconds: 60
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
      - type: Pods
        value: 1
        periodSeconds: 60
