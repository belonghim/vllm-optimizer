Plan-1 Issues
- None encountered beyond existing pre-existing static analysis warnings from the repo (not caused by these changes).
- If issues arise in CI due to environment type-checks, consider adjusting static analysis configuration or applying minimal, targeted fixes in a separate PR.
